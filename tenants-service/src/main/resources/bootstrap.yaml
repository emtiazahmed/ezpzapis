server:
  port: 8082
spring:
  application:
    name: tenants-service
  config:
    import: "optional:consul:"
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
---
server:
  port: 8082
spring:
  profiles: docker
  application:
    name: tenants-service
  cloud:
    consul:
      host: host.docker.internal
      port: 8500
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always